export class Partenaire {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  id_part: number;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  societe: string;
  mail: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  Fax: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  tel: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  codePostal: number;
}

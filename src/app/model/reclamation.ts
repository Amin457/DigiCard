export class Reclamation {
   // eslint-disable-next-line @typescript-eslint/naming-convention
   id_rec: number;
   // eslint-disable-next-line @typescript-eslint/naming-convention
   id_part: number;
   // eslint-disable-next-line @typescript-eslint/naming-convention
   id_client: number;
   // eslint-disable-next-line @typescript-eslint/naming-convention
   sujet_rec: string;
   description: string;
}
